program DelphiRestClient;

uses
  System.StartUpCopy,
  FMX.Forms,
  fmrMain_u in 'fmrMain_u.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
